export interface FormType {
  base: string;
  keyword: string;
  offset: number;
  length: number;
}

export interface EncodeResultType {
  default: string;
  advanced: string;
  special: string;
  issueTime: string;
}
